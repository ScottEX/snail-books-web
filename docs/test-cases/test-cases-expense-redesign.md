# ExpenseScreen 卡片式重设计 — 测试用例

## 一、卡片Tab切换
1. 页面加载后，默认显示「每日对账」卡片为激活态（红色左边框+阴影），内容区显示对账模块
2. 点击「营业额追踪」卡片 → 卡片激活态转移，内容区淡入显示营业额模块（300ms动画）
3. 点击「支出明细」卡片 → 同上切换
4. 点击回「每日对账」 → 对账数据保持（localStorage持久化不丢失）
5. 三张卡片水平排列，移动端可横向滚动不失位
6. 每张卡片显示对应摘要数据（对账显示差额、营业显示今日营收、支出显示记录数）

## 二、FadeIn 微交互动效
7. 切换tab时，内容区执行 opacity 0→1 + translateY(10px→0) 动画，时长300ms
8. 首次加载时内容区也有入场动画
9. 快速连续点击不同tab，动画不冲突（取消前一个动画）

## 三、NumberTicker 数字滚动动画
10. 对账差额从 0 滚动到真实金额，500ms ease-out
11. 渠道汇总金额滚动动画
12. 账面余额滚动动画
13. 营业额KPI数字滚动动画
14. 输入框改值后，差额重新计算并触发滚动动画
15. 日期切换（加载不同日期的localStorage数据）后，数字重新滚动

## 四、输入聚焦动效
16. 所有 TextInput 在 focus 时 borderColor 200ms 过渡到 #8B1E22
17. blur 时 borderColor 200ms 过渡回 #E5E7EB
18. 渠道小输入框同样适用

## 五、功能完整性（对账模块）
19. 日期选择器可切换日期
20. 卡余额输入 → onBlur 保存到 localStorage
21. 现金输入 → onBlur 保存到 localStorage
22. 5渠道各自独立输入 → onBlur 保存
23. 渠道汇总自动计算 = 堂食+美团+饿了吗+团购+京东
24. 账面余额 = 卡余额 + 现金
25. 对账差额 = 账面余额 - 渠道汇总
26. 差额正数/零 → 绿色 #059669
27. 差额负数 → 红色 #DC2626
28. 不同日期的数据隔离（切换日期后显示该日期的旧数据）

## 六、功能完整性（营业额模块）
29. 显示今日营业额KPI卡片
30. 显示实际营业额收入KPI卡片
31. 收入明细表有日期/分类/金额三列
32. 无数据时显示「暂无数据」

## 七、功能完整性（支出模块）
33. 日期+金额+说明三字段录入
34. 确认记账按钮 → POST /api/transactions
35. 记账成功后清空金额和说明字段，刷新列表
36. 金额为空时按钮置灰不可点击
37. 支出流水列表倒序显示（最新在上）
38. 每条显示：说明（左）+ 日期（小字灰）+ 金额（右，红色）

## 八、边界情况
39. localStorage 损坏时页面不崩溃（try/catch）
40. API 请求失败时页面不崩溃
41. 输入非数字 → toNum 返回 0
42. 金额输入小数点多位 → 正常计算
43. 空输入 → 显示 ¥0.00
44. 不同tab切换后回到对账tab，输入值保留
